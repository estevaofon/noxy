// check_strlen.nx
func main() -> void
    let s: string = "abc"
    // print("length: " + to_str(length(s)))
    // print("strlen: " + to_str(strlen(s)))
    
    // Check length
    let l: int = length(s)
    print("length('abc') = " + to_str(l))
end

main()
